%h1 News Room
#category-bar
  = link_to "All", root_path
  - @categories.each do |category|
    |
    = link_to category.name, category_path(category)
%hr

#article-list
  - @articles.each do |article|
    %h3= link_to article.headline, article_path(article)
